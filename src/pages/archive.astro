---
import ArticleArchiveLayout from '@/layouts/ArticleArchiveLayout.astro'
import { getArchives } from '@/utils/articles'

const archiveMap = await getArchives()
---

<ArticleArchiveLayout archiveMap={archiveMap} title='アーカイブ' />
